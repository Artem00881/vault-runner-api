{
  "name": "vault-runner-api",
  "version": "0.0.1",
  "private": true,
  "description": "Vault Run crash game backend — NestJS, server-authoritative, provably fair",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun run src/main.ts",
    "test": "bun test",
    "ws:smoke": "bun scripts/ws-smoke.ts",
    "load:test": "bun scripts/load-test.ts",
    "prisma:generate": "bunx prisma generate",
    "prisma:migrate": "bunx prisma migrate dev",
    "prisma:deploy": "bunx prisma migrate deploy",
    "prisma:studio": "bunx prisma studio"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.4",
    "@nestjs/config": "^3.2.3",
    "@nestjs/core": "^10.4.4",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.4.4",
    "@nestjs/platform-socket.io": "^10.4.4",
    "@nestjs/throttler": "^6.5.0",
    "@nestjs/websockets": "^10.4.4",
    "@prisma/client": "^5.20.0",
    "@sentry/bun": "^10.55.0",
    "@socket.io/redis-adapter": "^8.3.0",
    "helmet": "^8.2.0",
    "ioredis": "^5.4.1",
    "pg": "^8.21.0",
    "prom-client": "^15.1.3",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "socket.io": "^4.8.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "@types/pg": "^8.20.0",
    "bun-types": "^1.3.14",
    "prisma": "^5.20.0",
    "socket.io-client": "^4.8.0",
    "typescript": "^5.6.0"
  },
  "//overrides": "audit H4: pin patched versions of unreachable transitive deps (no file uploads / no lodash code / no qs.stringify in our code). @nestjs/core moderate has no 10.x fix and is accepted with rationale (CI gate fails on high+ only).",
  "overrides": {
    "multer": "2.1.1",
    "lodash": "^4.17.23",
    "qs": "^6.15.2",
    "file-type": "^21.3.1"
  }
}
